import 'package:dawara/db/sql.dart';
import 'package:dawara/functions/common.dart';

/*insertOrUpdate(name,value){
  var res =offlineDb().select("Select * from setting where name=?",[name]);
  if(res == null){
    offlineDb().select("Insert into setting(Name) values ()",[name]);
  }

}*/